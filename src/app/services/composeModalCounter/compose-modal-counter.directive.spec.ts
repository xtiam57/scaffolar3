import { ComposeModalCounterDirective } from './compose-modal-counter.directive';

describe('ComposeModalCounterDirective', () => {
  it('should create an instance', () => {
    const directive = new ComposeModalCounterDirective();
    expect(directive).toBeTruthy();
  });
});
